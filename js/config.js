/**
 * config.js
 *
 * The only value you need to edit after deploying/updating the Apps
 * Script backend (see README.md). No Google Cloud project, no OAuth
 * Client ID, nothing else required — sign-in is a plain emailed code
 * handled entirely by Apps Script + MailApp.
 */
const CONFIG = {
  // Paste the URL you get after "Deploy > Manage deployments" in the
  // Apps Script editor. It looks like:
  // https://script.google.com/macros/s/AKfycbxxxxxxxxxxxxxxxxxxxxxxxxxx/exec
  APPS_SCRIPT_URL: 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE'
};
