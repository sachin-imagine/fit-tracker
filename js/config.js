/**
 * config.js
 *
 * The only two values you need to edit after deploying the Apps
 * Script backend (see README.md, Step 3). Keep this file NOT secret
 * beyond "don't publish it somewhere random" — since this is a static
 * frontend, anything here is visible to anyone who has your GitHub
 * Pages URL. The token just keeps casual/accidental access out; it is
 * not meant to withstand a determined attacker (see DESIGN.md,
 * Security & Privacy section).
 */
const CONFIG = {
  // Paste the URL you get after "Deploy > New deployment > Web app" in
  // the Apps Script editor. It looks like:
  // https://script.google.com/macros/s/AKfycbxgMqWW3QmWltdaAwSfYEXPADDN2bYhDY2kcOLOzzFjfzucVcCHvP0JOhWuyGGs5CT2lQ/exec
  APPS_SCRIPT_URL: 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE',

  // Any random string — must match the API_SHARED_TOKEN Script
  // Property you set in the Apps Script editor.
  API_TOKEN: '1IWG2jTGCBWoBuIP2Ks5hKJjZrTpKJIRUcEzIaEUoppo'
};
